## hello md

```ts
let hero: string = "1";
```

```ts
export default defineConfig(async ({ command, mode }) => {
  const data = await asyncFunction();
  return {
    // vite config
  };
});
```

<HelloWorld></Helloworld>
